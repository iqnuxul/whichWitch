# 🚀 启动和测试指南

## ✅ 所有连接已配置完成！

你的前端、后端和智能合约已经正确连接。以下是详细说明：

---

## 📦 1. 安装依赖

```bash
npm install
```

---

## 🔧 2. 环境变量配置

所有环境变量已在 `.env.local` 中配置：

✅ **Web3 配置**
- Alchemy RPC URL (Sepolia)
- 合约地址 (CreationManager, PaymentManager, AuthorizationManager)

✅ **Supabase 配置**
- 项目 URL
- Anon Key
- Service Role Key

✅ **Pinata IPFS 配置**
- API Key
- API Secret
- JWT Token

---

## 🏃 3. 启动开发服务器

```bash
npm run dev
```

服务器将在 `http://localhost:3000` 启动

---

## 🧪 4. 测试连接

### 方法 1: 使用测试页面（推荐）

访问测试页面查看所有连接状态：
```
http://localhost:3000/test-connection
```

这个页面会显示：
- ✅ Web3 钱包连接状态
- ✅ 智能合约连接状态
- ✅ Supabase 数据库连接状态
- ✅ Pinata IPFS 配置状态
- ✅ 所有环境变量检查

### 方法 2: 手动测试

#### 测试 Web3 连接
1. 打开浏览器开发者工具 (F12)
2. 确保安装了 MetaMask
3. 连接钱包并切换到 Sepolia 测试网
4. 检查控制台是否有错误

#### 测试 Supabase 连接
在浏览器控制台运行：
```javascript
// 测试数据库连接
fetch('/api/test-supabase')
```

#### 测试智能合约
在应用中尝试读取合约数据，检查是否能正常获取

---

## 🔍 5. 检查 tsconfig.json 问题

### 问题原因
TypeScript 配置文件的 `include` 路径需要指向正确的源代码目录。

### 已修复
- ✅ 根目录 `tsconfig.json` 配置为 Next.js 项目
- ✅ `src/contracts/tsconfig.json` 配置为 Hardhat 项目
- ✅ 排除了不相关的目录

### 验证修复
```bash
# 清理缓存
rm -rf .next tsconfig.tsbuildinfo

# 重新启动开发服务器
npm run dev
```

如果仍有错误，可以忽略，因为 `next.config.mjs` 中已设置：
```javascript
typescript: {
  ignoreBuildErrors: true,
}
```

---

## 📊 6. 架构概览

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                              │
│                  (http://localhost:3000)                 │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              Next.js 前端 (src/ui/)                      │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Web3Provider (Wagmi + React Query)              │   │
│  │  - 钱包连接 (MetaMask)                            │   │
│  │  - 网络配置 (Sepolia)                             │   │
│  └──────────────────────────────────────────────────┘   │
└───┬─────────────────┬─────────────────┬────────────────┘
    │                 │                 │
    │                 │                 │
    ▼                 ▼                 ▼
┌─────────┐    ┌──────────┐    ┌──────────────┐
│ Alchemy │    │ Supabase │    │    Pinata    │
│   RPC   │    │ Database │    │     IPFS     │
└────┬────┘    └──────────┘    └──────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│   Sepolia 测试网智能合约              │
│  ┌─────────────────────────────┐   │
│  │  CreationManager.sol        │   │
│  │  - 作品注册                  │   │
│  │  - 衍生作品管理              │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │  PaymentManager.sol         │   │
│  │  - 支付处理                  │   │
│  │  - 收益分配                  │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │  AuthorizationManager.sol   │   │
│  │  - 授权管理                  │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 🎯 7. 关键文件位置

### 前端配置
- `src/ui/lib/web3/config.ts` - Wagmi 配置
- `src/ui/lib/web3/contracts/addresses.ts` - 合约地址
- `src/ui/lib/web3/contracts/abis.ts` - 合约 ABI
- `src/ui/lib/supabase/client.ts` - Supabase 客户端
- `src/ui/components/providers/web3-provider.tsx` - Web3 Provider

### 后端配置
- `src/backend/supabase/schema.sql` - 数据库 Schema
- `.env.local` - 环境变量（不提交到 Git）

### 智能合约
- `src/contracts/src/*.sol` - Solidity 合约
- `src/contracts/hardhat.config.cjs` - Hardhat 配置

---

## ⚠️ 8. 常见问题

### Q1: MetaMask 连接失败
**解决方案:**
1. 确保 MetaMask 已安装
2. 切换到 Sepolia 测试网
3. 刷新页面重新连接

### Q2: 合约读取失败
**解决方案:**
1. 检查合约地址是否正确
2. 确保连接到 Sepolia 网络
3. 验证 RPC URL 是否可用

### Q3: Supabase 连接错误
**解决方案:**
1. 确认 `.env.local` 中的 URL 和 Key 正确
2. 在 Supabase Dashboard 运行 `schema.sql`
3. 检查 Supabase 项目是否处于活动状态

### Q4: TypeScript 错误
**解决方案:**
```bash
# 清理缓存
rm -rf .next node_modules/.cache

# 重新安装
npm install

# 重启开发服务器
npm run dev
```

---

## 🎉 9. 下一步

现在你可以：

1. ✅ 访问主页: `http://localhost:3000`
2. ✅ 测试连接: `http://localhost:3000/test-connection`
3. ✅ 连接 MetaMask 钱包
4. ✅ 开始开发功能

### 获取测试币
访问 Sepolia 水龙头获取测试 ETH：
- https://sepoliafaucet.com/
- https://www.alchemy.com/faucets/ethereum-sepolia

### 部署新合约（如需要）
```bash
# 编译合约
npm run compile

# 部署到 Sepolia
npm run deploy:sepolia
```

---

## 📝 10. 提交前检查

在提交到比赛前，请运行：
```bash
# 检查 Git 状态
git status

# 确保 .env.local 不在列表中
# 清理构建文件
rm -rf .next out build
```

参考 `提交前检查.md` 获取完整清单。

---

**🎊 恭喜！你的项目已经完全配置好了！**
